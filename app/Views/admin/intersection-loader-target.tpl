<div class="intersection-loader-target flex justify-center items-center" data-url="{{url}}"
    data-page="{{pagination_page}}" data-limit="{{pagination_limit}}" data-search="{{search_value}}"
    data-sort="{{sort_type}}" data-order="{{sort_order}}">
    <div class="spinner-grow inline-block w-8 h-8 bg-sky-500 rounded-full opacity-0" role="status"></div>
</div>